# iFlow Agent 启动脚本使用说明

## 脚本列表

### 1. `start_all_with_logs.bat` - 推荐使用 ⭐
- **功能**: 启动 Java 后端和 React 前端，每个服务在独立的窗口中运行
- **优点**: 日志清晰，方便调试，支持热加载
- **使用场景**: 日常开发，需要同时查看前后端日志

### 2. `start_all_single_window.bat` - 单窗口模式
- **功能**: 所有服务在后台运行，日志输出到文件，可在同一窗口查看
- **优点**: 节省窗口空间，日志统一管理
- **使用场景**: 窗口空间有限，需要统一查看日志

### 3. `stop_all.bat` - 停止所有服务
- **功能**: 停止 Java 后端和 React 前端
- **使用**: 双击运行即可

## 快速开始

### 启动服务（推荐方式）

```bash
# 双击运行
start_all_with_logs.bat
```

### 停止服务

```bash
# 双击运行
stop_all.bat
```

## 访问地址

启动成功后，可以通过以下地址访问：

- **前端界面**: http://localhost:5173
- **后端 API**: http://localhost:8080
- **API 文档**: http://localhost:8080/swagger-ui.html
- **健康检查**: http://localhost:8080/actuator/health

## 热加载功能

### Java 后端热加载
- **技术**: Spring Boot DevTools
- **触发方式**: 修改 Java 源文件后自动重新编译
- **生效时间**: 通常 2-5 秒
- **支持**: 代码修改、静态资源更新

### React 前端热加载
- **技术**: Vite HMR (Hot Module Replacement)
- **触发方式**: 修改前端文件后自动刷新
- **生效时间**: 毫秒级
- **支持**: 代码修改、样式修改、组件更新

## 日志查看

### 独立窗口模式 (`start_all_with_logs.bat`)

启动后会自动打开两个命令行窗口：
- **Java Backend 窗口**: 显示后端编译和运行日志
- **React Frontend 窗口**: 显示前端编译和开发服务器日志

### 单窗口模式 (`start_all_single_window.bat`)

日志输出到文件：
- **后端日志**: `logs/backend.log`
- **前端日志**: `logs/frontend.log`

查看日志命令：
```bash
# 查看后端日志
type logs\backend.log

# 查看前端日志
type logs\frontend.log

# 实时监控（使用 PowerShell）
Get-Content logs\backend.log -Wait -Tail 50
Get-Content logs\frontend.log -Wait -Tail 50
```

## 端口配置

默认端口：
- **后端**: 8080
- **前端**: 5173

如需修改端口，编辑对应的 `.bat` 文件：
```batch
set "BACKEND_PORT=8080"
set "FRONTEND_PORT=5173"
```

## 故障排查

### 端口被占用

如果提示端口被占用：
1. 运行 `stop_all.bat` 停止现有服务
2. 或者手动修改端口号

### 后端启动失败

检查：
1. Java 版本是否为 17 或更高
2. Maven 是否正确安装
3. 依赖是否完整（运行 `mvn clean install`）

### 前端启动失败

检查：
1. Node.js 版本是否为 18 或更高
2. 依赖是否完整（运行 `npm install`）
3. 端口是否被占用

## 开发建议

### 推荐工作流程

1. **启动服务**: 运行 `start_all_with_logs.bat`
2. **开发修改**: 修改代码后自动热加载
3. **查看日志**: 在对应的窗口中查看日志
4. **完成开发**: 运行 `stop_all.bat` 停止服务

### 代码修改建议

**Java 后端**:
- 推荐使用 IDE（如 IntelliJ IDEA）的自动编译功能
- 大范围修改后可以手动重启确保生效

**React 前端**:
- 小修改会自动刷新
- 大组件更新可能需要手动刷新浏览器（F5）

## 技术栈

### 后端
- **框架**: Spring Boot 3.2.0
- **Java 版本**: 17
- **构建工具**: Maven
- **热加载**: Spring Boot DevTools

### 前端
- **框架**: React 18
- **构建工具**: Vite 7
- **包管理**: npm
- **热加载**: Vite HMR

## 注意事项

1. **首次启动**: 前端需要运行 `npm install` 安装依赖
2. **内存占用**: 两个服务同时运行会占用较多内存（建议 8GB+）
3. **日志清理**: 日志文件会持续增长，定期清理 `logs` 目录
4. **浏览器兼容**: 推荐使用 Chrome、Edge 或 Firefox 最新版本

## 常用命令

### 手动启动（不使用脚本）

**启动后端**:
```bash
cd backend-java
mvn spring-boot:run
```

**启动前端**:
```bash
cd frontend
npm run dev
```

### 查看进程

```bash
# 查看端口占用
netstat -ano | findstr "8080"
netstat -ano | findstr "5173"

# 终止进程
taskkill /F /PID <进程ID>
```

## 更新日志

### v1.0.0 (2026-02-09)
- ✅ 创建独立窗口启动脚本
- ✅ 创建单窗口启动脚本
- ✅ 创建停止脚本
- ✅ 支持热加载功能
- ✅ 完善日志输出

---

**最后更新**: 2026-02-09
**维护者**: iFlow Agent Team